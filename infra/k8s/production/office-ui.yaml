apiVersion: apps/v1
kind: Deployment
metadata:
  name: office-ui
  namespace: autoswarm
  labels:
    app.kubernetes.io/name: office-ui
    app.kubernetes.io/part-of: autoswarm-office
    app.kubernetes.io/component: frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: office-ui
  template:
    metadata:
      labels:
        app.kubernetes.io/name: office-ui
        app.kubernetes.io/part-of: autoswarm-office
        app.kubernetes.io/component: frontend
    spec:
      serviceAccountName: autoswarm-sa
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        fsGroup: 1001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: office-ui
          image: ghcr.io/madfam-org/autoswarm-office-ui@sha256:d147cde24484330b89a353ef63254925c105c229decee56f7db4677e497b5bd9
          ports:
            - containerPort: 3000
              protocol: TCP
          resources:
            requests:
              cpu: 25m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          startupProbe:
            httpGet:
              path: /api/health
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 3
            failureThreshold: 30
          readinessProbe:
            httpGet:
              path: /api/health
              port: 3000
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /api/health
              port: 3000
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          securityContext:
            allowPrivilegeEscalation: false
            privileged: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          env:
            - name: NEXT_PUBLIC_API_URL
              value: https://api.selva.town
            - name: NEXT_PUBLIC_COLYSEUS_URL
              value: wss://ws.selva.town
            - name: NEXT_PUBLIC_JANUA_ISSUER_URL
              value: https://auth.selva.town
            - name: NEXT_PUBLIC_JANUA_CLIENT_ID
              value: autoswarm-office
            - name: NEXT_PUBLIC_DEMO_ENABLED
              value: "true"
            - name: NEXT_PUBLIC_GUEST_ACCESS_ENABLED
              value: "true"
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            - name: next-cache
              mountPath: /app/.next/cache
      volumes:
        - name: tmp
          emptyDir: {}
        - name: next-cache
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: office-ui
  namespace: autoswarm
  labels:
    app.kubernetes.io/name: office-ui
    app.kubernetes.io/part-of: autoswarm-office
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: office-ui
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
