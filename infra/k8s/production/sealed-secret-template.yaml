apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: autoswarm-secrets
  namespace: autoswarm
  labels:
    app.kubernetes.io/part-of: autoswarm-office
spec:
  encryptedData:
    database-url: SEALED_VALUE
    redis-url: SEALED_VALUE
    janua-client-secret: SEALED_VALUE
    anthropic-api-key: SEALED_VALUE
    openai-api-key: SEALED_VALUE
    colyseus-secret: SEALED_VALUE
    dhanam-webhook-secret: SEALED_VALUE
  template:
    metadata:
      name: autoswarm-secrets
      namespace: autoswarm
      labels:
        app.kubernetes.io/part-of: autoswarm-office
    type: Opaque
